import styled from "styled-components";

const Info = styled.dl`
  margin-top: 16px;

  dt {
    font-size: 14px;
    font-weight: 500;
    color: #1f2937;
  }

  dd {
    font-size: 16px;
    font-weight: 700;
    color: #1f2937;
    margin-top: 6px;
  }
`;

export default Info;
